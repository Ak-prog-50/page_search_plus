{
    "name": "page-search-plus",
    "scripts": {
        "install-all": "cd frontend && yarn install && cd ../backend && yarn install",
        "dev-run": "cd backend && yarn start-dev && cd ../frontend && yarn dev"
     }
  }
  